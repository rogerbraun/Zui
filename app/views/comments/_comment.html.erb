<div class="comment">
  <div class="comment_head"><strong><%= image_tag comment.user.gravatar_link %> <%= comment.user.email %></strong>, <%= comment.updated_at %>
    <div class="comment_actions">
      <% if can? :destroy, comment %>
        <%= link_to "x", [comment.fragment.project ,comment.fragment, comment], :method =>"delete", :remote => true, :confirm => "Wirklich löschen?" %>
      <% end %>
    </div>
  </div>
  <p><%= comment.body %></p>
</div>
